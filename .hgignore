syntax: glob

*~
*.pyc
.*swp
templates/*.zip
templates/*.log
templates/moderncv/*.out
templates/moderncv/*.log
templates/moderncv/*.aux
*.sqlite*
.DS_Store
*.orig
*.png
syntax: regexp
#we need the directory STORE because it is looked for when creating the subdirs 
#but we don't need every individual's data in it :)
^\STORE/[0-9]*/

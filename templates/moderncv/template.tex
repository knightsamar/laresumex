{% load some_tags %}
\documentclass{article}

\begin{document}
    \large{ {{s.fullname}} } \\
    \textit{ Phone: }{{s.phone_number}}\\
    \textbf{ EMAIL: } {{s.email}}\\
    {% if s.sex == 'm' %}
      Male , 
    {% else %}
       Female ,
    {% endif %}
    {{ s.birthdate|date|timesince }}\\
\hline
    \\
    \\
    \section*{Career Objective}
\hline
    \textit{ {{s.career_objective}} }
    \\
    \section*{Personality Traits}
      \begin{itemize}
	{% for per in p.strength|split:',' %}       
	  \item { {{per}} }
	{% endfor %}
      \end{itemize}

  {% if  m %}
 \section*{Academic Qualification }
\hline
      \begin{tabular}{llll}
  
       Class & Board/University &  Percentage & Year&\\
      {% for marks in m %}
	  {{ marks.course }} & {{ marks.uni }} & {{ marks.marks }} & {{ marks.fromDate }}&\\
      {% endfor %}
      \end{tabular}
 {% endif %}

 {% if c %}
 \section*{ Certification }
\hline
      \begin{tabular}{lll}
  
       NAme & Month and Year&\\
      {% for certi  in c %}
	  {{ certi.desc }} & {{ certi.fromDate|date:'M   Y' }} &\\
      {% endfor %}
      \end{tabular}
 {% endif %}

\end{document}
